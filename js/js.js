function sample() {
	
	alert("OK!")

	var ajaxRequest;
	
	/*
	try{ajaxRequest = new XMLHttpRequest();}
	catch(e){try{ajaxRequest = new ActiveXObject("Msxml2.XMLHTTP");}
	catch(e){try{ajaxRequest = new ActiveXObject("Microsoft.XMLHTTP");}
	catch(e){alert("Browser problem.");return false;}}}
		
	ajaxRequest.onreadystatechange = function(){
		if(ajaxRequest.readyState == 4){
			document.getElementById('element_id').innerHTML = ajaxRequest.responseText;   //ID name of the element Where to display the result of the PHP Script
		}
	}
	
	querySTR = "?user=" + user +  "&pass=" + pass + "&priv=" + priv;				//Values to be pass in the php Script
		ajaxRequest.open("GET", "createuser2.php" + querySTR, true);					//Name of the PHP Script to Call
	ajaxRequest.send(null); 
	
	*/
}

